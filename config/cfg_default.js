config = {
// this is not JSON just JavaScript (idea is taken from github.com/tldrio/mongo-edit)
    mongodb: {
         host: 'localhost'
        ,port: 27017
        ,journal: true
        ,dbname: 'test'
        ,dbpath: 'data/db'
        ,dbprefix: ''
    }
    ,gsm_modules: [
        {
            //name
            //addr:
        }
    ]
    ,app: {
    }
}
